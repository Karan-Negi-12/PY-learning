#Ques Write a program to calculate the area of a triangle. Take the base and height as input from the user.

Height = float(input("Enter Height of Triangle: "))
Base = float(input("Enter Base of Triangle: "))

a = 1/2 * (Height * Base)

print(f"Area of Trinagle: {a}")